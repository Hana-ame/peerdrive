# Peerdrive 使用手册

## 界面概览

```
┌────────── 侧边栏 ──────────┬────────── 主面板 ────────────────────────────┬─ 版本历史 ─┐
│                            │                                             │            │
│ Peerdrive                  │ alice / my_project                          │ 版本历史    │
│                            │ 工作区 (待提交变更)                           │            │
│ ┌─ 节点配置 ──────────┐   │                                             │ v1 12:30   │
│ │ 当前节点用户: alice  │   │ ┌────────────────────────────────────────┐  │ 添加了...  │
│ │ 当前合集: my_project│   │ │ 类型 │ 路径        │ SHA256       │ 操作││            │
│ └──────────────────────┘   │ │ 📄  │ main.go     │ a1b2c3...    │ 下载││ v0 12:00   │
│                            │ │ 📄  │ utils.py    │ d4e5f6...    │ 移除││ 初始提交   │
│ ┌─ Fork 远端 ──────────┐   │ └────────────────────────────────────────┘  │            │
│ │ 远端用户名: bob       │   │                                             │            │
│ │ 远端合集名: my_project│   │ [ + 上传文件 ]  [ Commit: ________ ] [✓]   │            │
│ │ [Fork to Local]       │   │                                             │            │
│ └──────────────────────┘   │                                             │            │
│                            │                                             │            │
│ ┌─ Merge 合并 ─────────┐   │                                             │            │
│ │ 源用户名: bob         │   │                                             │            │
│ │ 源合集名: my_project  │   │                                             │            │
│ │ 策略: Ours ▼          │   │                                             │            │
│ │ [Merge into Local]    │   │                                             │            │
│ └──────────────────────┘   │                                             │            │
│                            │                                             │            │
│ ┌─ P2P 网络 ───────────┐   │                                             │            │
│ │ ID: 12D3KooW...      │   │                                             │            │
│ │ [已连接 peers]        │   │                                             │            │
│ └──────────────────────┘   │                                             │            │
└────────────────────────────┴─────────────────────────────────────────────┴────────────┘
```

---

## 基本概念

### 节点
一个 Peerdrive 节点 = 一台运行 Peerdrive 服务端程序的机器。每个节点有一个唯一的 `peer_id`（类似身份证号），通过 P2P 网络连接到其他节点。

### 用户（命名空间）
每个节点下可以管理多个用户。用户只是一个名字（`alice`、`bob`），没有密码。**切换用户名就可以查看/操作该用户下的所有数据。**

### 合集（Collection）
合集是一个文件集合，类似 Git 仓库。一个用户可以有多个合集（如 `my_project`、`docs`、`photos`）。

### 文件条目
合集内的每个文件记录，包含：
- **路径**：文件在合集中的相对路径（如 `src/main.go`）
- **文件哈希**：SHA256 哈希，唯一标识文件内容

### 版本（Version）
每次 Commit 会将当前工作区的所有文件条目打一个快照，生成版本号（v0, v1, v2...）。可以回滚到任意历史版本。

---

## 操作指南

### 切换用户
1. 在左侧 **当前节点用户** 输入框中键入用户名
2. 按键盘 Enter 或点击空白处
3. 主面板自动加载该用户下的合集

### 切换合集
1. 在 **当前合集** 输入框中键入合集名
2. 如果合集不存在，上传文件时会自动创建

### 上传文件
1. 点击右上角 **+ 上传文件** 按钮
2. 选择要上传的文件
3. 文件会上传到服务端存储，同时作为条目添加到当前合集的工作区
4. **注意：上传后数据进入工作区，需要 Commit 才能保存为版本**

### 下载文件
在文件列表中，点击文件右侧的 **下载** 链接，浏览器会下载该文件。

### 移除文件
点击文件右侧的 **移除** 按钮，该条目会从工作区删除。此操作不会删除服务端的文件存储，只是从合集的工作区中移除引用。

### 提交版本（Commit）
1. 在右上角输入框中填写提交信息（如"添加了登录功能"）
2. 点击 **Commit** 按钮（绿色 ✓）
3. 当前工作区的所有文件条目会被保存为一个新版本
4. 右侧版本历史列表会立即更新

### 查看版本历史
右侧 **版本历史** 面板展示该合集的所有 Commit 记录，按时间倒序排列，显示：
- 版本号（v0, v1, v2...）
- 提交时间
- 提交信息

### 回滚版本
1. 鼠标悬停在版本历史中的某个版本上
2. 点击右上角出现的 **↩ Rollback** 按钮
3. 确认后，该版本的快照会恢复到当前工作区
4. 页面自动刷新

### Fork（从远端复制合集）

Fork 是从远端节点复制一个合集到当前用户下。

1. 在左侧 **Fork 远端** 区域填写：
   - **远端用户名**：对方节点上的用户名
   - **远端合集名**：要复制的合集名
2. 点击 **Fork to Local** 按钮
3. Fork 成功后，当前合集会被覆盖为远端合集的内容

**说明**：Fork 会从 P2P 网络中拉取远端节点的数据，需要远端节点在线且已连接。

### Merge（合并远端合集到本地）

Merge 是将远端合集的变更合并到本地合集中。

1. 在左侧 **Merge 合并** 区域填写：
   - **源用户名**：对方节点上的用户名
   - **源合集名**：要合并的合集名
2. 选择冲突解决策略：
   - **Ours**：冲突时保留本地的版本
   - **Theirs**：冲突时采用远端的版本
   - **Manual**：遇到冲突时报错，由用户手动处理
3. 点击 **Merge into Local** 按钮
4. 如果选 Manual 策略且有冲突，会弹窗列出所有冲突文件的路径

### P2P 网络管理

底部 **P2P 网络** 区域显示：
- **节点 ID**：当前节点的 peer_id
- **已连接 peers**：通过 P2P 连接到当前节点的其他节点列表
- **刷新按钮**：手动刷新节点和 peers 信息

---

## 典型工作流

### 单人使用
```
1. 输入用户名 → 输入合集名
2. 上传文件 → 文件出现在工作区
3. 修改文件 → 重新上传 → 更新条目
4. Commit → 保存版本
5. 继续修改 → 再次 Commit → 版本递增
```

### 多人协作（Forks）
```
Alice 节点                            Bob 节点
├── alice/my_project                  ├── bob/my_project
│    ├── main.go (v0→v1→v2)           │    ├── main.go (v0→v3)
│    └── utils.py                      │    └── config.json
                                      │
Alice Fork Bob:                        │
1. 填写 远端用户=bob, 远端合集=my_project
2. Fork → alice/my_project = bob/my_project
   (现在也有 config.json 了)

Alice Merge Bob:                       │
1. 填写 源用户=bob, 源合集=my_project, 策略=Ours
2. Merge → Bob 的新文件合并到本地
```

---

## 常见问题

**Q: 上传后文件不见了？**
A: 文件被成功存储，但要 Commit 才能永久保存为版本上传后的文件在工作区是可见的，但如果切换到其他合集再切回来，未 Commit 的变更不会消失（工作区是持久的）。

**Q: 为什么合并没有生效？**
A: 确认远端节点在线且已连接到当前节点。在 **P2P 网络** 区域查看是否有 peers 列表。

**Q: **Q: 如何创建一个新的合集？**
A: 在 **当前合集** 输入框中输入一个新名称，然后上传一个文件，合集会自动创建。
