# Peerdrive 测试报告

> 2026-05-03T16:30:00 · 前端移动端适配 + 创建页功能修复

---

## 总览

| 类别 | 测试数 | 状态 |
|------|--------|------|
| Go 单元测试 | 9 | ✅ 9/9 PASS |
| 前端 Vitest | 27 | ✅ 27/27 PASS |
| Playwright 移动端冒烟 | 6 | ✅ 6/6 PASS |
| 前端编译 | — | ✅ 零错误 |

---

## 1. Go 单元测试 (9 用例)

### 运行方式

```bash
cd back && go test -tags nosqlite ./internal/service/ -run "TestCreateCollection" -v
```

### 测试覆盖

| 测试 | 验证点 |
|------|--------|
| `TestCreateCollection_ValidEntries` | 正常条目创建成功，hash 格式正确 |
| `TestCreateCollection_PathTraversal` | `../` 路径穿越拒绝 |
| `TestCreateCollection_InvalidHash` | 非法 hash 的 provider 拒绝 |
| `TestCreateCollection_EmptyEntries` | 空条目合集创建成功 |
| `TestCreateCollection_EmptyPath` | 空路径拒绝 |
| `TestCreateCollection_DirEntryWithoutProvider` | **新**: 目录条目（路径结尾 `/`）无需 provider |
| `TestCreateCollection_FileWithoutProvider` | **新**: 文件条目没有 provider 仍拒绝 |
| `TestCreateCollection_AbsolutePath` | 绝对路径拒绝 |
| `TestGetCollection_ByHash/NonesistentHash` | 合集查询 |

### 后端修改

| 文件 | 改动 |
|------|------|
| `internal/service/anon_service.go` | CreateCollection/CommitCollection 跳过目录条目的 provider 验证 |

---

## 2. 前端 Vitest (27 用例, 3 文件)

### 运行方式

```bash
cd front && npx vitest run
```

### 测试文件

| 文件 | 用例 | 说明 |
|------|------|------|
| `tests/smoke.test.jsx` | 4 | 组件渲染不崩溃 |
| `tests/components.test.jsx` | 15 | 组件渲染 + 交互 + **新增 7 用例** |
| `tests/FileTree.test.jsx` | 8 | 文件树渲染 |

### 新增测试

| 测试 | 验证点 |
|------|--------|
| `EditorToolbar > 保存按钮在无有效条目时禁用` | disabled=true |
| `EditorToolbar > 保存按钮在有有效条目时可用` | disabled=false |
| `EditorToolbar > 标签输入在独立行` | 标签 placeholder 存在 |
| `EditorToolbar > 显示文件计数` | `3 个文件` 文本 |
| `FileTree > 空状态显示拖拽提示` | 无条目时提示 |
| `FileTree > 新建文件夹按钮存在` | 按钮渲染 |
| `FileTree > 点击新建文件夹显示内联输入框` | 点击后出现输入框 |

---

## 3. Playwright 移动端冒烟 (6/6)

### 运行方式

```bash
node /home/lumin/.claude/skills/playwright-test/scripts/test-runner.mjs <test-file>
```

### 测试结果

| # | 测试 | 断言 |
|---|------|------|
| 1 | 设置页移动端加载 | 节点连接标题可见 |
| 2 | 移动端标签栏可见 | 节点连接/认证/存储管理 按钮 |
| 3 | 桌面侧边栏隐藏 | aside 不可见 |
| 4 | 汉堡菜单按钮存在 | aria-label="菜单" |
| 5 | 汉堡菜单展开显示导航项 | 菜单含导航链接 |
| 6 | 创建页移动端面板标签 | 文件/编辑器 标签可见 |

---

## 4. 前端编译

```bash
cd front && npx vite build
```

- 零错误
- 产物: `dist/` (JS ~538KB)

---

## 本轮改动清单

### 前端

| 文件 | 改动 |
|------|------|
| `SystemBrowse.jsx` | 文件"+"先注册再添加，文件夹"+"展开并注册内容后添加 |
| `EditorToolbar.jsx` | 标签移至第二行独立显示 |
| `FileTree.jsx` | 新建文件夹改为内联输入，右键重命名支持文件夹 |
| `LeftPanel.jsx` | 传递新的 onSysAddFile/onSysAddFolder 回调 |
| `index.jsx` | 新增 handleSysAddFile/handleSysAddFolder 异步处理 |

### 后端

| 文件 | 改动 |
|------|------|
| `anon_service.go` | 跳过目录条目的 provider 验证 |

---

## 测试文件清单

```
front/
├── tests/
│   ├── components.test.jsx       # 15 用例（含新增 7）
│   ├── smoke.test.jsx            # 4 用例
│   └── FileTree.test.jsx         # 8 用例
back/
├── internal/service/
│   └── anon_service_test.go      # 9 用例（含新增 2）
```
