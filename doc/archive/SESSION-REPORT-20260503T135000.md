# Session Report — 多后端切换 + P2P 协议测试

> 生成时间: 2026-05-03T13:50:00+08:00
> 分支: main
> Commits: 6 个

## 1. P2P 协议测试工具

**文件**: `back/cmd/p2p-test/main.go`

裸 go-libp2p 测试工具，不依赖 peerdrive 内部包，直接操作 stream 层。

### 支持的协议与操作

| Protocol ID | 操作 | 说明 |
|---|---|---|
| `/peerdrive/collections/list/1.0.0` | list / query / exists | 合集发现 — 列出、查询、判断存在 |
| `/peerdrive/exchange/1.0.0` | get / size | 文件交换 — 下载、查询大小 |

### 实机测试结果 (VPS bwh.moonchan.xyz)

| 操作 | 结果 |
|---|---|
| `--op list` | ✅ 6 个匿名合集 |
| `--op query "Relay"` | ✅ OR 语义命中 |
| `--op exists "DHT Test"` | ✅ 存在 |
| `--op size <relay.txt>` | ✅ 19 字节 |
| `--op get <relay.txt>` | ✅ 下载 + SHA256 验证通过 |

## 2. 测试矩阵

**文件**: `doc/testing/P2P-PROTOCOL-TEST.md`

覆盖 3 大类、40+ 测试用例：
- Collections List (L-01 ~ L-23)
- Exchange (E-01 ~ E-15)
- 连接层 (N-01 ~ N-06)

## 3. 多后端切换 (前端)

**涉及文件**: `front/src/api.js`, `front/src/pages/Settings.jsx`, `front/src/__mocks__/api.js`

### 功能
- 预设两个后端：**WSL** (`wsl-3000.moonchan.xyz`) + **BWH** (`97.64.30.221:3000`)
- 按钮式快速切换，当前后端高亮
- 添加自定义后端（弹窗输入名称+URL）
- 临时连接任意地址（不保存）
- 当前连接只读展示 + 节点信息卡片
- STUN/TURN 配置按后端存储，切换时自动同步

### 修复
- 修复 CI 测试失败：mock API 缺少后端管理导出

## 4. CI/CD 状态

| Workflow | 状态 |
|---|---|
| Peerdrive CI | ✅ success |
| Go Build Matrix | ✅ success |
