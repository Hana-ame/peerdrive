# Database
peerdrive.db
*.db

# Compiled binaries
peerdrive-server
peerdrive
*.exe
*.out

# Compiled binaries (back)
back/server
back/bt-integration

# OS files
.DS_Store
Thumbs.db
/server
/main
downloads/

# 集成测试运行产物：config.Load() 默认 StorageDir=./storage，
# 部分用例未覆盖该字段，于是按 cwd 落在包目录下生成内容寻址块。
back/test/integration/storage/
back/test/integration/downloads/

*.bin

# Node
node_modules/
dist/
# 例外：公共面板产物是**交付物**（单文件，用户直接双击/托管用），必须入库。
# 其余 dist/ 仍然忽略（构建缓存、media 的打包产物等）。
!packages/peerdrive-client/dist/
!packages/peerdrive-client/dist/panel.html

# Archives
*.zip

# Claude config (personal)
.claude/

# Agent 工作区记忆/缓存（本地数据，不入库；与其他 agent 配置同类处理）
.workbuddy/

# Git worktrees
worktrees/
frontend-worktrees/

# Separate services (not in this repo)
registration-server/
p2p-dual-stack/

# Upload state
.last_upload_url

# 验证脚本产物（线上自检截图，每次跑都会变）
packages/peerdrive-client/pages-verify.png

# Root temp files
# 注意：这些是「仓库根目录」的临时目录，必须加 / 锚定。
# 写成 react/ / go/（不带前导 /）会匹配**任意层级**的同名目录——
# 2026-09-20 就是因此把 packages/peerdrive-media/src/react/ 整个吞掉，
# 导致该包的 React 源码从未入库、CI 的 `npm run build` 找不到入口。
*.txt
/react/
/go/
/docs/
